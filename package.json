{
  "name": "lti-moodle-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Portable LTI 1.3 Provider module for Express + Vue 3. Lets your app be launched from Moodle (or any LTI 1.3 LMS) with OIDC, Deep Linking, session bridging, and platform registration.",
  "keywords": [
    "lti",
    "lti-1.3",
    "moodle",
    "canvas",
    "blackboard",
    "ltijs",
    "express",
    "vue",
    "deep-linking",
    "education"
  ],
  "author": "HKU TELI",
  "license": "UNLICENSED",
  "main": "./backend/dist/index.js",
  "types": "./backend/dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./backend/dist/index.d.ts",
      "default": "./backend/dist/index.js"
    },
    "./backend": {
      "types": "./backend/dist/index.d.ts",
      "default": "./backend/dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p backend/tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('backend/dist',{recursive:true,force:true})\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ltijs": "^5.9.9",
    "jsonwebtoken": "^9.0.2",
    "express": "^4.19.0"
  },
  "peerDependencies": {
    "axios": "^1.6.0",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "peerDependenciesMeta": {
    "axios": { "optional": true },
    "vue": { "optional": true },
    "vue-router": { "optional": true }
  },
  "optionalDependencies": {
    "mongoose": "^8.0.0",
    "drizzle-orm": "^0.30.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "notes": {
    "ltijs": "REQUIRED. The LTI 1.3 Provider engine. Stores its own platform/nonce state in MongoDB (LTI_DB_URL).",
    "jsonwebtoken": "REQUIRED. Used by the example adapters' generateJwt() to mint app-session JWTs. If your project already has its own JWT helper, you can drop this.",
    "express": "REQUIRED (in the host app). Listed here as a hard dep so the types match. The standalone module's API surface uses Express 4 types; hosts on Express 5 (like Learnity) bridge the gap with a one-line cast at the integration boundary — see backend/src/lti/index.ts.",
    "mongoose": "OPTIONAL. Only needed if you use the Mongoose adapter / Mongoose models.",
    "drizzle-orm": "OPTIONAL. Only needed if you use the Drizzle adapter.",
    "pg": "OPTIONAL. Postgres driver — only needed with Drizzle adapter (or other raw-SQL setup).",
    "vue / vue-router / axios": "OPTIONAL peer deps for the frontend bits. Required only if you copy the Vue components into your frontend project."
  }
}
